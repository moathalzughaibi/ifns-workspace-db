# IFNS Workspace DB

Welcome! In VS Code press **Ctrl+Shift+P → “Run Task”** and start with:

1. **START HERE (open guide)**
2. **Bootstrap**
3. **Doctor**
4. **Build SoT DBs**
5. **Import SoT Kits**

Docs are in `docs/` — the canonical entry is `docs/START_HERE_IFNS_SoT_Operations.md`.
