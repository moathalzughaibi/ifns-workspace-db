# IFNS Workspace DB

This repo mirrors the IFNS Workspace Notion database to CSV snapshots (`sync/workspace/`).
Run locally (with `local_env\workspace_env.ps1`) or via the GitHub Action (manual button).
